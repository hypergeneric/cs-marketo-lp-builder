{
	"name": "marketo-landing-builder",
	"type": "module",
	"scripts": {
		"build": "node builder.mjs",
		"watch": "node builder.mjs watch",
		"serve": "browser-sync start --server dist --files \"dist/**/*\" --no-open",
		"dev": "npm-run-all --parallel watch serve"
	},
	"dependencies": {
		"chokidar": "^4.0.0",
		"sass": "^1.80.0",
		"yaml": "^2.5.0"
	},
	"devDependencies": {
		"browser-sync": "^3.0.4",
		"npm-run-all": "^4.1.5"
	}
}
